# Quant Fix Pass — 2026-03-24

Issues fixed: 1 (NBA pyth HFA — replaced multiplicative ELO boost with additive ELO-space conversion), 2 (NBA MC noise — added `game_noise_std` parameter to `simulate_game()`, NBA calls use 11.5 instead of 14.0), 3 (zero features — removed always-zero `turnover_diff`/`turnover_diff_adjusted`/`turnover_rate_diff` from logistic and XGBoost feature vectors; added length assertions), 4 (stale weights — NFL ensemble weights cache now expires after 30 days), 5 (LR HFA double-count — removed embedded `+65` from logistic `elo_diff`; binary `home_field_advantage` feature handles HFA alone), 6 (weight feedback loop — minimum resolved games raised from 10 → 50, window from 20 → 50, nudge magnitude halved), 7 (ensemble weight optimization — removed misleading hardcoded `log: 0.5` / `xgb: 0.5` entries from NBA `sub_probs_hist`; also applied ELO-space pyth fix in historical loop), 8 (early-season badge — `isEarlySeason()` helper + "⚠ Early Season" badge in game cards for NBA Oct/early-Nov and NFL weeks 1-3), 9 (Bayesian signal clamp — `margin_multiplier` 25.0 → 15.0, clip bounds [800,2200] → [1200,1800]).

Files modified: `model/monte_carlo.py`, `model/logistic_model.py`, `model/ensemble_model.py`, `model/nfl_ensemble_weights.py`, `model/bayesian_model.py`, `scripts/update_nba_data.py`, `scripts/main.py`, `main.js`.
